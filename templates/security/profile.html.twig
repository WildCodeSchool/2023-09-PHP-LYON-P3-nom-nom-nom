{% extends 'base.html.twig' %}

{% block title %}Nom-Nom - Mon profil
{% endblock %}

{% block body %}
	<a class="link-offset-2 link-underline link-underline-opacity-0 nav-link fs-6 fw-semibold mt-3 mr-3" href="{{ path('app_logout') }}">
		Se déconnecter</a>
	<h1 class="text-center fs-1 mt-4">Mon profil</h1>
	<img class="w-50 object-fit-cover user-avatar" src="{{ asset('build/images/user.png') }}" alt="Photo de l'avatar Nom-Nom"/>
	<h4 class="text-center">Pseudo</h4>
	<p class="text-center">{{app.user.pseudo}}</p>
	<h4 class="text-center">Mail</h4>
	<p class="text-center">{{app.user.email}}</p>
	<div>
		<a href="#" type="button" class="btn button-crud mt-1 mb-5" tabindex="-1" role="button" aria-disabled="true">Mes favoris</a>
		<a href="#" type="button" class="btn button-crud mt-1 mb-5" tabindex="-1" role="button" aria-disabled="true">Mes recettes</a>
	</div>
{% endblock %}
