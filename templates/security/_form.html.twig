{# {{ form_start(form) }}
<div class="mt-3 grid-form gap-2">
	{{ form_row(form.lastname) }}
	{{ form_row(form.firstname) }}
	{{ form_row(form.pseudo) }}
	{{ form_row(form.email) }}
</div>
{{ form_end(form) }} #}

{{ form_start(form) }}
<div class="my-custom-class-for-errors">
	{{ form_errors(form) }}
</div>

<div class="container">
	<div>
		{{ form_row(form.lastname) }}
	</div>
	<div>
		{{ form_row(form.firstname) }}
	</div>
	<div>
		{{ form_row(form.pseudo) }}
	</div>
	<div>
		{{ form_row(form.email) }}
	</div>
	<button type="submit" class="btn button-crud mt-4 mb-2 mx-auto">Valider</button>
</div>

{{ form_end(form) }}
