{% extends 'base.html.twig' %}

{% block title %}
	Accueil - Bienvenue sur l'application Nom nom nom
{% endblock %}

{% block body %}
	<img
	class="w-100 object-fit-cover d-none d-sm-none d-md-block" src="{{ asset('/build/images/background-home.png') }}"/>

	{# last 3 recipies mobile #}
	<div class="container text-center mt-4 pt-3 d-none d-md-block d-lg-none">
		<div id="carouselExampleIndicators" class="carousel carousel-dark slide">
			<div class="carousel-inner">
				{% for key, recipe in showRecipes %}
					<div class="carousel-item {% if key == 0 %}{% endif %}">
						<a href="{{ path('app_recipe_show', { id: recipe.id }) }}"><img src="{{ asset('build/images/' ~ recipe.picture) }}" class="pic-recipe object-fit-cover" alt="{{ recipe.nameRecipe }}"/></a>
						<div class="container-fluid carousel-caption d-md-block glassmorphism">
							<h5>{{ recipe.nameRecipe }}</h5>
							<p class="w-75 container text-center">{{ recipe.description }}</p>
						</div>
					</div>
				{% endfor %}
			</div>

			{# left and right button #}
			<button class="carousel-control-prev d-none d-md-block d-lg-none" type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide="prev">
				<span class="carousel-control-prev-icon" aria-hidden="true"></span>
				<span class="sr-only">Previous</span>
			</button>
			<button class="carousel-control-next  d-none d-md-block d-lg-none" type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide="next">
				<span class="carousel-control-next-icon" aria-hidden="true"></span>
				<span class="sr-only">Next</span>
			</button>
		</div>
	</div>

	{# last 3 recipies desktop  #}
	<h2 class="pt-5 m-5 text-center ">Découvrez les 3 dernières recettes</h2>


	<div class="d-flex justify-content-evenly .d-none .d-lg-block .d-xl-none">
		{% for recipe in showRecipes %}
			<div class="position-relative mb-5 pb-">
				<a class="" href="{{ path('app_recipe_show', { id: recipe.id }) }}">
					<img src="{{ asset('build/images/' ~ recipe.picture) }}" class="pic-recipe-desktop object-fit-cover" alt="{{ recipe.nameRecipe }}"/>
				</a>
				<div class="glassmorphism-desktop m-2">
					<h5 class="p-2">{{ recipe.nameRecipe }}</h5>
					<p class="container text-center">{{ recipe.description }}</p>
				</div>
			</div>

		{% endfor %}
	</div>

	<div class="position-relative">
		<img class="w-100 img-fluid" src="{{ asset('/build/images/background.png') }}"/>
		<div class="container text-center position-absolute top-50 start-50 translate-middle">
			<h1>Rejoignez notre communauté</h1>
			<p class="paragraph">A chaque bouchée une nouvelle saveur.</p>
			<p class="container-fluid mt-4 px-4 paragraph">Rejoignez notre communauté culinaire et transformez chaque repas en une expérience mémorable, où la passion de la cuisine se marie harmonieusement avec le plaisir de créer des souvenirs délicieux.</p>
			<a href="#" type="button" class="btn button-crud mt-1" tabindex="-1" role="button" aria-disabled="true">Rejoignez-nous</a>
		</div>
	</div>

	<h3 class="pt-5 m-5 text-center">Explorez différentes recettes par catégories</h3>

	<div class="mx-auto container text-center m-5 p-5">
		<img class="w-25" src="{{ asset('/build/images/entree.png')}}"/>
		<img class="w-25" src="{{ asset('/build/images/plat.png')}}"/>
		<img class="w-25" src="{{ asset('/build/images/dessert.png')}}"/>
	</div>
{% endblock %}
