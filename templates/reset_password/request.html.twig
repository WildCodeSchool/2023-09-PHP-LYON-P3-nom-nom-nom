{% extends 'base.html.twig' %}

{% block title %}Mot de passe oublié
{% endblock %}

{% block body %}

	{# <img class="w-100 object-fit-cover d-none d-md-block" src="/build/images/background-pages.png"/>
	
		{% for flash_error in app.flashes('reset_password_error') %}
			<div class="alert alert-danger" role="alert">{{ flash_error }}</div>
		{% endfor %}
		<h1 class="text-center">Modifier votre mot de passe</h1>
	
		{{ form_start(requestForm) }}
		{{ form_row(requestForm.email) }}
		<div class="text-center">
			<small>
				Merci de rentrer votre adresse mail et nous vous enverrons un lien
				                afin de réinitialiser votre mot de passe.
			</small>
		</div>
	
		<button class="btn btn-primary">Envoyer la demande de modification</button>
		{{ form_end(requestForm) }} #}

	{# -----------------------------fond mobile------------------------------ #}
		<div class="bg-image h-100" style="background-image: url('build/images/fond_login.png');"> <div class="bg-fond h-100">
			{% for flash_error in app.flashes('reset_password_error') %}
				<div class="alert alert-danger" role="alert">{{ flash_error }}</div>
			{% endfor %}
			<div class="d-flex flex-column align-items-center vh-100">
				<div class="d-flex h-75 flex-column align-items-center justify-content-md-evenly">
					<img src="/build/images/logo_white_500.png" alt="logo de nomnomnom" class="logo-add "/>
					<h1 class="text-center password-lost">Modifier votre mot de passe</h1>

					{{ form_start(requestForm) }}
					{{ form_row(requestForm.email) }}
					<div class="text-center">
						<small>
							Merci de rentrer votre adresse mail et nous vous enverrons un lien
															                afin de réinitialiser votre mot de passe.
						</small>
					</div>

					<button class="btn btn-primary">Envoyer la demande de modification</button>
					{{ form_end(requestForm) }}
				</div>
			</div>
		</div>


		{# ----------------------------desktop------------------------------------ #}
		{# <div class="container-fluid p-0 m-0">
			<div class="row row-cols">
				<img src="/build/images/photo_register.jpg" alt="photo d'un plat" class="d-none d-md-block object-fit-cover back-picture col-md-5 p-0"/>
				<div class="col-md-7 p-0 ">
					<div class="bg-desktop-login bg-image h-100 w-100" style="background-image: url('/build/images/background_login.png');">
						{% for flash_error in app.flashes('reset_password_error') %}
							<div class="alert alert-danger" role="alert">{{ flash_error }}</div>
						{% endfor %}
						<div class="d-flex h-75 flex-column align-items-center justify-content-md-evenly">
							<img src="/build/images/logo_white_500.png" alt="logo de nomnomnom" class="logo-add "/>
							<h1 class="text-center">Modifier votre mot de passe</h1>

							{{ form_start(requestForm) }}
							{{ form_row(requestForm.email) }}
							<div class="text-center">
								<small>
									Merci de rentrer votre adresse mail et nous vous enverrons un lien
																	                afin de réinitialiser votre mot de passe.
								</small>
							</div>

							<button class="btn btn-primary">Envoyer la demande de modification</button>
							{{ form_end(requestForm) }}
						</div>
					</div>
				</div>
			</div>
		</div> #}
	{% endblock %}
